{
  "name": "weddingwallet",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Wedding expense tracker — React + Vite SPA on Cloudflare Pages, Supabase Postgres backend.",
  "scripts": {
    "dev": "vite",
    "dev:api": "npm run build && wrangler pages dev --port 8788",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "cf:preview": "npm run build && wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "hono": "^4.6.3",
    "jose": "^5.9.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241004.0",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.2",
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "wrangler": "^3.80.0"
  }
}
